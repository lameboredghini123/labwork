#WAP to calculate & print the sum of the first ten positive integers as a range

#declaring a range containing the first ten positive integers and storing them in a variable 'list1'
list1=range(1,11)

#using the sum() function which will directly output the sum of all the elements in the range and printing it
print(sum(list1))
